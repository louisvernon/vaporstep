# Media

Place the end-user gameplay video at:

```text
docs/media/vaporstep-demo.mp4
```

The root README already links to this path.

If the video becomes large, consider hosting it in a GitHub Release or on a video service and replacing the README link rather than keeping a large binary in Git history.
